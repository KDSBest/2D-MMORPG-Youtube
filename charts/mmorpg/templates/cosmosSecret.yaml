apiVersion: v1
kind: Secret
metadata:
  name: CosmosSecret
  annotations:
    kubernetes.io/service-account.name: "sa-name"
type: kubernetes.io/service-account-token
data:
  CosmosUrl: {{ .Values.cosmosUrl }}
  CosmosKey: {{ .Values.cosmosKey }}
